apiVersion: apps/v1
kind: Deployment
metadata:
  name: aws-mcp
spec:
  replicas: {{ .Values.services.awsMcp.replicas }}
  selector:
    matchLabels:
      app: aws-mcp
  template:
    metadata:
      labels:
        app: aws-mcp
    spec:
      serviceAccountName: agentic-service-account
      containers:
      - name: aws-mcp
        image: {{ .Values.registry }}/{{ .Values.services.awsMcp.image }}
        ports:
        - containerPort: {{ .Values.services.awsMcp.port }}
        resources:
          requests:
            cpu: {{ .Values.resources.requests.cpu }}
            memory: {{ .Values.resources.requests.memory }}
          limits:
            cpu: {{ .Values.resources.limits.cpu }}
            memory: {{ .Values.resources.limits.memory }}
        livenessProbe:
          httpGet:
            path: /health
            port: {{ .Values.services.awsMcp.port }}
          initialDelaySeconds: 30
        readinessProbe:
          httpGet:
            path: /health
            port: {{ .Values.services.awsMcp.port }}
          initialDelaySeconds: 5

---
apiVersion: v1
kind: Service
metadata:
  name: aws-mcp-service
spec:
  selector:
    app: aws-mcp
  ports:
  - port: 80
    targetPort: {{ .Values.services.awsMcp.port }}
  type: ClusterIP