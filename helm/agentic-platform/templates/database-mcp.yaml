apiVersion: apps/v1
kind: Deployment
metadata:
  name: database-mcp
spec:
  replicas: {{ .Values.services.databaseMcp.replicas }}
  selector:
    matchLabels:
      app: database-mcp
  template:
    metadata:
      labels:
        app: database-mcp
    spec:
      containers:
      - name: database-mcp
        image: {{ .Values.registry }}/{{ .Values.services.databaseMcp.image }}
        ports:
        - containerPort: {{ .Values.services.databaseMcp.port }}
        resources:
          requests:
            cpu: {{ .Values.resources.requests.cpu }}
            memory: {{ .Values.resources.requests.memory }}
          limits:
            cpu: {{ .Values.resources.limits.cpu }}
            memory: {{ .Values.resources.limits.memory }}
        livenessProbe:
          httpGet:
            path: /health
            port: {{ .Values.services.databaseMcp.port }}
          initialDelaySeconds: 30
        readinessProbe:
          httpGet:
            path: /health
            port: {{ .Values.services.databaseMcp.port }}
          initialDelaySeconds: 5

---
apiVersion: v1
kind: Service
metadata:
  name: database-mcp-service
spec:
  selector:
    app: database-mcp
  ports:
  - port: 80
    targetPort: {{ .Values.services.databaseMcp.port }}
  type: ClusterIP